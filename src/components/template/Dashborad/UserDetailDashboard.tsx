import { User_Interface } from '@/types/modelTypes';
import React from 'react';

const UserDetailDashboard = ({user}: {user: User_Interface}) => {
    return (
        <div>
            
        </div>
    );
};

export default UserDetailDashboard;