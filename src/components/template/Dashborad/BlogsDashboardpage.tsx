import { Agent_Interface, Blog_Interface, User_Interface } from '@/types/modelTypes';
import React from 'react';

const BlogsDashboardpage = ({ blogs , authors }: {blogs: Blog_Interface[] , authors: User_Interface[] | Agent_Interface[]}) => {
    return (
        <div>
            
        </div>
    );
};

export default BlogsDashboardpage;